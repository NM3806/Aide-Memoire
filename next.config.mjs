/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: [
      'img.freepik.com',
      'images.unsplash.com',
      'images.pexels.com'
    ],
  },
};

export default nextConfig;
